<div class="mx-auto md:w-2/3 w-full flex">
  <div class="bg-white shadow-xl overflow-hidden sm:rounded-lg">
    <div class="grid grid-cols0-2">
      <div class="px-4 py-5 sm:px-6">
        <div class="flex">
          <!--
            TODO Add drop down item, move to the right, remove space between text
            figure out how to get XML to pretty print
            probably a library for XML but if not build one
            section top borders should not go all the way to end
          -->
          <h3 class="text-lg leading-6 font-medium text-gray-900">
            Request Summary | <%= @call.name %>
          </h3>
          <a class=" shadow-xl inline  w-3/12 flex items-center justify-center px-2 mx-auto py-1
                      border border-transparent text-lg font-medium rounded-md text-white bg-blue-500
                      hover:bg-blue-400">
            Download
          </a>
        </div>
        <p class="mt-1 max-w-2xl text-sm text-gray-500"><%= @call.created_at %></p>
      </div>
    </div>
    <div class="border-t border-gray-200">
      <dl>
        <div class="border bg-white" id="request">
          <div class="container mx-auto m-3">
            <a class="m-3 shadow-xl w-3/12 flex items-center justify-center px-2 mx-auto py-2
                      border border-transparent text-lg font-medium rounded-md bg-gray-100
                      hover:bg-gray-200">
              Request Details
            </a>
          </div>
          <div class="px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Request Endpoint</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @call.req_endpoint %></dd>
          </div>
          <% if @call.req_body %>
            <div class="px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
              <dt class="text-sm font-medium text-gray-500">Request Body</dt>
              <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                <%= @call.req_body %>
              </dd>
            </div>
          <% end %>
        </div>
        <div class="border bg-white" id="response">
          <div class="px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Response Status</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @call.resp_status %></dd>
          </div>
          <div class="px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
            <dt class="text-sm font-medium text-gray-500">Response Body</dt>
            <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
              <%= @call.resp_body %>
            </dd>
          </div>
        </div>
      </dl>
    </div>
  </div>

</div>
